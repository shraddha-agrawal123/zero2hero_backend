# --- Flask & Extensions ---
Flask==3.0.3
Flask-Cors==4.0.1
Flask-PyMongo==2.3.0
Werkzeug==3.1.3   # optional (installed automatically with Flask, but safe to pin)

# --- ML / AI ---
tensorflow==2.17.1
numpy==1.26.4
matplotlib==3.9.2
pillow==10.4.0
h5py==3.13.0
gdown==4.7.1       # for downloading model from Google Drive

# --- Computer Vision ---
opencv-python==4.10.0.84
scikit-image==0.24.0

# --- Database ---
pymongo==4.9.2
dnspython==2.6.1   # needed for MongoDB Atlas SRV URLs
bson==0.5.10       # optional (comes with pymongo, but safe to include)

# --- Geolocation ---
geopy==2.4.1

# --- Utilities ---
wrapt==1.17.2      # auto-installed with TensorFlow, optional to pin
zstandard==0.24.0  # optional, used for compression (pulled in by ML libs)
pip>=24.0
setuptools>=68.0
wheel>=0.42
